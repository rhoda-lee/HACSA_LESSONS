Modules
Another advantage of using functions in python is the ability to keep 
functions in files which are known as modules
Keeping functions in separate files help you to keep the code and focus 
on its hig level logics
You can share modules with otherss and they can call your functions

use import keyword and module name to import the entire module
use the from keyword plus the import keyword and the specific package or functions
to make a specific import

Alias
Assigning aliases

Built-in modules
import math
import os